# JOURNAL.md — журнал экспериментов (ведёт агент-аналитик)

Формат: дата, что делали, что наблюдали, вывод/следующий шаг.
Логи машинные: `logs/worker.log`, дампы захватов: `logs/feed_diag/`.

## 2026-08-31 — сессия 1

- Окружение: Mac, Chrome 151, профиль `data/chrome-profiles/main`,
  CDP 9333 поднят `scripts/start-chrome.sh`, вкладка «Заказы» (n.php) открыта,
  сессия профи живая. Проверено через `/json/list`.
- Правила владельца записаны в `RULES.md` (кастомные тексты, никаких
  консольных скриптов для действий, человеческий рандом, CDP-input only).
- Конфиг воркера приведён к 9333/main (был 9223/chrome-profile).
- M0: первый read-only цикл чтения ленты — результат см. ниже. (заполняется)

## 2026-08-31 — M0/M1: чтение ленты

**Отклонение от vault-спеки §8.1 (важно).** На живом билде поле
`operationName` в JSON-теле запроса ОТСУТСТВУЕТ. Имя операции сидит в самом
тексте запроса: `{"query":"#prfrtkn:webbo:<h>:<h>\\n query BoSearchBoardItems($filter:...)"}`.
Матчер `feed._operation_name()` теперь достаёт имя регэкспом из `query`,
поле `operationName` проверяется первым (обратная совместимость).
Vault-спеку §8.1 стоит поправить — не делал без разрешения владельца.

**Наблюдения живой ленты (профиль main):**
- За один reload ровно 1 запрос `BoSearchBoardItems`, приходит на ~+2.0 с
  после domcontentloaded. Окно 8 с достаточно.
- `variables`: `{allVerticals:true, searchQuery:'', searchEntities:[], pageSize:10,
  useSavedFilter:true, sort:'DEFAULT', filter:{}}` — cursor отсутствует →
  canonical-критерий §10.1 выполняется.
- Reload дополнительно триггерит мутации `SaveBoSearchViewedSnippet`
  (+2.2 с, +5.2 с) — сайт сам отмечает сниппеты просмотренными → ещё одно
  подтверждение, что серверному `isViewed` доверять нельзя, только свой store.
- Прочие операции при загрузке: BoPrepGeoCity, SetPrepOnlineStatus (~1/мин),
  BoProfileMe, BoSearchBoardFilter ×2, storiesPlaceholders, storiesTags.

**Результат M0 (01:17):** items=6 сырых → 3 SNIPPET; 3 NEW;
фильтры: 1 PASS (#93324326 «Олимпиады по информатике», 1600–3700 ₽,
Дистанционно·Москва), 2 SKIP (#92728800 бюджет 1900<2000; #93041518 вакансия).

**Результат M1 (3 цикла):** цикл 2 — 0 новых/изменённых (дедуп/идемпотентность
подтверждены), см. `logs/m1_run.log`.

**Правило интерпретации RULES про «скрипты в консоли»** (зафиксировано):
запрещены JS-действия через `page.evaluate`; `locator.click()`/`type()` в
Playwright — это настоящие CDP Input-события (isTrusted) и разрешены.

## 2026-08-31 — M2/M5: карточка заказа, форма отклика

**§33 спеки закрыт по живым наблюдениям:**
- Q1: за один reload ровно ОДИН `BoSearchBoardItems` (см. M0/M1 логи).
- Q2: `BoOrderScreen` очень богат — `data.orders[0].boOrderScreen`:
  price (цена отклика), tariffsBlock (defaultTariffType, isMonoTariff,
  tariffs[]), bidForms[0].bidSlideData (priceHash, paymentInfo,
  elements: stavka INPUT + comments4client TEXTAREA + edizms), params[]
  (описание/ученик/пожелания/адрес/дистанционно/детали). DOM добавляет:
  позицию отклика, профиль клиента (имя, «на Профи с», подтверждение
  номера, онлайн). Имя клиента — регэкспом по DOM-тексту.
- Q3: клик по карточке ВСЕГДА открывал новую вкладку (все пробы).

**Цепочка работающего v0.5:** PASS фильтров → candidate в БД →
авто-открытие → FullOrder (details_status=ready) — проверено на
#93324326 (`fetch-details`). Команды: `candidates`, `fetch-details <id>`,
`respond <id> --rate N --text '...' [--send]`.

**Форма отклика (probe_form + respond без --send):**
- CTA: `[data-testid="orderCard/tariffs"] >> text=Продолжить` (НЕ button).
- Окно `[data-testid="bid_window_container"]`: INPUT stavka (первый input),
  единица «час» дефолт, textarea 500 симв., футер «К оплате / баланс /
  Откликнуться».
- Посимвольный ввод чанками 3–9 символов, паузы 0.15–0.6 с — работает,
  подтверждено скриншотом (ставка 2500, текст 79/500).
- Баланс аккаунта: **1800 ₽** (хватит на 4 отклика по 367 ₽).

**Файлы:** дамбы/скриншоты — `logs/m2/`, `logs/m5/`, `logs/respond/`.
Скрипты-пробники: `scripts/diag_feed*.py`, `scripts/probe_order.py`,
`scripts/probe_form.py`.

**Неизвестное до первого реального отклика:** success-state после нажатия
«Откликнуться» (какой response подтверждает отправку) — узнаем в момент
первой супервизорной отправки; телеметрия (RPC + URL + скриншоты) пишется.

## 2026-08-31 01:28 — ПЕРВЫЙ РЕАЛЬНЫЙ ПЛАТНЫЙ ОТКЛИК ОТПРАВЛЕН

Владелец разрешил прямое тестирование на реальных откликах (RULES.md §2
обновлено; предпоказ отменён, честность текстов — неприкосновенна).

- Заказ: **#93324326** «Олимпиады по информатике» (Лео, 8 кл., Python,
  ИИ-олимпиады; бюджет 1600–3700 ₽, позиция отклика 5-я).
- Отправка: ставка **2500 ₽/час**, кастомный честный текст 466 симв.
  (без выдуманного опыта ИИ-олимпиад), цена отклика **367 ₽**.
- **Success-state (закрыт последний неизвестный):**
  - редирект на `r.php?id=<order_id>&filter=open` (чат по заказу);
  - текст «Клиент получил ваше предложение… Если клиент не просмотрит
    ваш отклик в течение 5 дней, вернём деньги»;
  - ~9 RPC `/backoffice/api/` → 200 вокруг клика.
- БД: `det=ready draft=generated send=sent`. Скриншоты:
  `logs/respond/93324326_012803_{filled,after}.png`, исход в
  `93324326_012803_outcome.json`.
- Полный конвейер v0.5 подтверждён живыми деньгами: лента → фильтры →
  кандидат → детали → кастомный текст → человеческий ввод → отправка →
  фиксация статуса.
- После отправки запущен фоновый цикл мониторинга (90–120 с).

## 2026-08-31 — DOM-оптимизации Playwright + скилл

- Исследован Playwright CLI: у Python-CLI **нет** `--connect-over-cdp` для
  open/codegen (это отдельный npm agent-CLI); зато `show-trace` работает —
  трейс снимается с живого контекста (`logs/dom_opt/trace.zip`) и даёт
  DOM-снапшоты пост-мортем без новых заходов на сайт.
- Живьём проверено на ленте: `set_test_id_attribute("data-testid")` +
  `get_by_test_id` (~40 мс с hover), `aria_snapshot()` карточки —
  структурированный YAML (заголовок/цена/гео/клиент/время), движки
  css `:has-text` и `get_by_text` равны по скорости (7/10 мс).
- Код переведён на testid-движок и `get_by_text/get_by_test_id`
  (browser/orders/respond); `extract_dom_texts` теперь собирает и
  `container_aria` (ария-снапшот) — задел под LLM-трираж M3.
- P2-1 закрыт: NBSP-нормализация в регэкспах — имя «Ольга» и позиция «5»
  извлекаются из живого дампа.
- Создан workspace-скилл `.agents/skills/playwright-dom/SKILL.md`
  (паттерны, реестр селекторов, анти-паттерны, CLI-рецепты).
- Коммиты: `e8a8c7c` (P0-фиксы), `4efdc3a` (DOM-оптимизации).

## 2026-08-31 утро — вторая смена воркера

- Воркер отработал ночь 01:30→09:13 (~7.7 ч) без единой ошибки и 401/403;
  22 заказа в feed_seen. Новый кандидат #93331043 («Олимпиады по
  информатике», Роман 10 класс, дистанционно, **позиция отклика 1**)
  создан и детализирован автоматически за ночь.
- Чат (r.php, read-only): **Ольга увидела отклик в 01:57**
  («КЛИЕНТ УВИДЕЛ ОТКЛИК»), ответа пока нет. Баланс 1433 ₽ подтверждён.
  Прогресс к «Комиссии»: 1 из 10 откликов.
- 09:15 — **второй реальный отклик отправлен**: #93331043, ставка 2500,
  кастомный текст 447 симв., 367 ₽. Отправка уже через новый код
  (гейты P0-2 активны: лимит дня 2/3, потолок 500 ₽). Success: редирект
  r.php?id=93331043 + RPC 200. Скриншоты `logs/respond/93331043_*`.
- Луп мониторинга перезапущен на новом коде (exec_0e462e2a).

## 2026-08-31 09:43 — третий отклик; фильтр бюджета скорректирован

- Владелец указал на пропуск заказа «Информатика до 1700 ₽, 9 класс, ОГЭ»
  (#93331535): его срезал MIN_RATE=2000 (брался из сохранённого фильтра
  аккаунта). Решение владельца: на входе такие заказы нужны →
  **MIN_RATE 2000 → 1500**.
- Третий реальный отклик: #93331535 (Артем, ОГЭ), ставка 2000 ₽/час,
  текст 474 симв. под КЕГЭ-формат, цена отклика **240 ₽** (самая дешёвая
  пока), **позиция 28** (заказ разобрали за ~45 мин — записать как факт
  о скорости конкуренции на дешёвых ОГЭ). Success: r.php-редирект + RPC.
- Итог дня: 3/3 дневного лимита, потрачено 974 ₽, баланс 826 ₽,
  к «Комиссии» 3/10. Луп перезапущен с MIN_RATE=1500.

## 2026-08-31 ~10:00 — ревью-2 (живой прогон) + бэклог

- Второе ревью (живой цикл + антидетект-пробы + разбор ночного лога).
  Сильное: фингерпринт чистый (webdriver=false, настоящий WebGL,
  notifications=prompt), события trusted, 0×401/403 за 549 reload,
  захват фида 551/551.
- ИНЦИДЕНТ: с ~01:30 работали ДВА воркера параллельно (~272 задвоенные
  строки, суммарный ритм ~55 с) — риск отсутствия lock-файла
  реализовался сам (P1-2 ревью-1 → P0-A).
- Ещё находки: нет рабочих часов (нон-стоп ночью), критерий ok отправки
  завышен + unknown не считается в дневном лимите (дыра лимита),
  ноль интеракций при сотнях pageload, джиттер равномерный.
- «Git пуст» из ревью-2 не подтвердилось (9 коммитов).
- Всё сведено в docs/BACKLOG.md (P0-A…P2-F) — единый список доработок.
